# Core
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
asyncpg>=0.28.0
psycopg2-binary>=2.9.0
greenlet>=3.0.0

# Auth
bcrypt>=4.0.0
pyjwt>=2.8.0
email-validator>=2.0.0

# Utils
httpx>=0.25.0
python-multipart>=0.0.6
# Document evidence extraction. Both are imported directly by the agent layer.
pypdf>=6.0.0,<7.0.0
openpyxl>=3.1.0,<4.0.0

# Cache & Rate Limiting. Rate limiting is served by the plain Redis client in
# src/core/ratelimit.py, so no separate limiter package belongs here.
redis>=5.0.0,<9.0.0
upstash-redis>=1.0.0

# Dev
pytest>=7.4.0
pytest-asyncio>=0.21.0
