"""The Studies, as templates on the analysis compiler's own road.

A template is a plan of steps and a board written against their names. It is not
a privileged path: its reads go through the reader chat reads the store by, its
arithmetic goes through the sandbox and the validator a model's ``compute`` call
goes through, and its board compiles through the composer every board compiles
through. What a template still is, and a model is not, is *a question somebody
has already answered well* — the sequence, the shape and the sentence, written
once and cached as quality.

Registering one is an import. The imports at the bottom of this file are that
act; a template whose module nobody imports is absent from the catalog no matter
how correctly it is written.
"""

from __future__ import annotations

from . import earnings_dislocation  # noqa: F401  (imported for its side effect)
from . import entry_condition_review  # noqa: F401  (imported for its side effect)
from . import intraday_liquidity  # noqa: F401  (imported for its side effect)
from . import volume_at_price  # noqa: F401  (imported for its side effect)

__all__ = ["earnings_dislocation", "entry_condition_review", "intraday_liquidity", "volume_at_price"]
